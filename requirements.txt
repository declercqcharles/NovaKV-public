# torch is a CUDA 12.6 nightly build, not on standard PyPI -- install separately first:
#   pip install --pre torch==2.14.0.dev20260708+cu126 --index-url https://download.pytorch.org/whl/nightly/cu126
# then install the rest:
#   pip install -r requirements.txt

transformers==5.13.0
accelerate==1.14.0
datasets==5.0.0
huggingface_hub==1.23.0
tokenizers==0.22.2
safetensors==0.8.0
numpy==2.5.1

# eval harness + metrics (LongBench/RULER/HumanEval/MBPP/zero-shot)
lm_eval==0.4.12
evaluate==0.4.6
rouge==1.0.1
rouge_score==0.1.2
sacrebleu==2.6.0
nltk==3.10.0
jieba==0.42.1

tqdm
